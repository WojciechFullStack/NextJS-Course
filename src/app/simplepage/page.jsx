import Template from "../templates/Template";

const Page = () => (
  <Template>
    <section>
      <h2>This is a special page using the template!</h2>
      <p>Some content here...</p>
    </section>
  </Template>
);

export default Page;